/**
 * 三大平台入口卡片区块插槽（旅游社区 / 景点攻略 / AI 助手）。
 * 空占位，待 `homepage-feature-nav` change 填充。
 */
export default function FeatureNavSlot() {
  return <section data-region="feature-nav" aria-label="feature-nav placeholder" />;
}
