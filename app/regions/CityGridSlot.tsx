/**
 * 热门目的地推荐区块插槽（4-6 张城市卡片）。
 * 空占位，待 `homepage-city-grid` change 填充。
 */
export default function CityGridSlot() {
  return <section data-region="city-grid" aria-label="city-grid placeholder" />;
}
