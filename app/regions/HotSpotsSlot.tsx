/**
 * 热门景点 / POI 榜单区块插槽（承接"景点攻略"入口）。
 * 空占位，待 `homepage-hot-spots` change 填充。
 */
export default function HotSpotsSlot() {
  return <section data-region="hot-spots" aria-label="hot-spots placeholder" />;
}
