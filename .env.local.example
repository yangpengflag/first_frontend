# 复制本文件为 .env.local 并按需修改（.env.local 已被 .gitignore 忽略，禁止提交）
#
# 前端运行时后端地址。
# Next.js 在 build / 启动阶段读取 NEXT_PUBLIC_* 变量，并内联到客户端 bundle，
# 因此 SSR 阶段与 hydration 后的客户端 fetch 共用同一份地址，避免「首屏正常、续拉失败」。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
